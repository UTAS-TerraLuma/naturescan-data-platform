# API: STAC

The STAC API is done using [stac-fastapi-geoparquet](https://github.com/stac-utils/stac-fastapi-geoparquet). 
It only requires a collections.json or collection.parquet be set. So no code is included in this directory.
